version: 1
backend:
  phases:
    build:
      commands:
        - echo "no build phase"
        # - npm ci --cache .npm --prefer-offline
        # - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
frontend:
  phases:
    preBuild:
      commands:
        - echo "preBuild phase"
        - echo "VITE_GOOGLE_MAPS_API_KEY=$VITE_GOOGLE_MAPS_API_KEY" >> .env
        - echo "VITE_NEW_API=556789" >> .env
        - cat .env
    build:
      commands:
        - echo "frontend build phase"
        # - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*